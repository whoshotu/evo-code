# ========================================
# EvolveCode Environment Variables
# ========================================
# 
# IMPORTANT: This is a template file. Copy this to .env and fill in your values.
# DO NOT commit .env to version control!
#
# Quick Setup:
#   cp .env.example .env
#   Edit .env and add your API keys
#
# ========================================

# ----------------------------------------
# AI API Configuration
# ----------------------------------------
# API keys are configured in backend/config/config.php (NOT here)
# This keeps your keys secure on the server
#
# SECURITY WARNING:
# - API keys should NEVER be in frontend code
# - Backend proxy keeps keys secure
# - Set usage limits in your provider's console
# - Restrict to your domain in production
#
# We use OpenRouter for unified AI access
# Get your key: https://openrouter.ai/keys

# Custom API Endpoint (if using a different provider)
# API_BASE_URL=https://api.yourprovider.com/v1

# ----------------------------------------
# Deployment Configuration
# ----------------------------------------

# Deployment Platform (vercel, netlify, altervista, github-pages, custom)
DEPLOYMENT_PLATFORM=vercel

# Base URL for production deployment
# Examples:
#   - Root domain: https://yourdomain.com
#   - Subdirectory: https://yourdomain.com/evo-code
#   - AlterVista: https://yourusername.altervista.org
PUBLIC_URL=http://localhost:3000

# Base path for routing (leave empty for root, or /subdirectory/)
# Used by Vite's base config
# Examples:
#   - Root: /
#   - Subdirectory: /evo-code/
BASE_PATH=/

# ----------------------------------------
# Backend API Proxy (Recommended for Production)
# ----------------------------------------
# If using a backend proxy to secure your API keys:
# This is HIGHLY RECOMMENDED to prevent API key exposure
#
# For AlterVista deployment:
# VITE_BACKEND_URL=https://yourusername.altervista.org/backend/api/ai-proxy.php
#
# For local development:
# VITE_BACKEND_URL=http://localhost/backend/api/ai-proxy.php
VITE_BACKEND_URL=https://yourusername.altervista.org/backend/api/ai-proxy.php
USE_BACKEND_PROXY=true

# ----------------------------------------
# Development Configuration
# ----------------------------------------

# Development server port (default: 3000)
DEV_PORT=3000

# Enable development features
DEV_MODE=true

# ----------------------------------------
# Analytics (Optional)
# ----------------------------------------
# Uncomment if using Google Analytics
# GA_TRACKING_ID=G-XXXXXXXXXX

# Uncomment if using other analytics
# MIXPANEL_TOKEN=your_token_here
# AMPLITUDE_API_KEY=your_key_here

# ----------------------------------------
# Feature Flags (Optional)
# ----------------------------------------
# Enable/disable features for testing
# ENABLE_VOICE_OUTPUT=true
# ENABLE_VIDEO_GENERATION=false
# ENABLE_IMAGE_GENERATION=true
# ENABLE_AI_TUTOR=true

# ----------------------------------------
# API Rate Limiting (Optional)
# ----------------------------------------
# Client-side rate limiting (requests per minute)
# API_RATE_LIMIT=10
# API_TIMEOUT=30000

# ----------------------------------------
# DEPLOYMENT-SPECIFIC INSTRUCTIONS
# ----------------------------------------

# === VERCEL DEPLOYMENT (Recommended) ===
# 1. Install Vercel CLI: npm install -g vercel
# 2. Login: vercel login
# 3. Link project: vercel link
# 4. Add environment variables:
#    vercel env add GEMINI_API_KEY
#    (Paste your API key when prompted)
#    Select: Production, Preview, Development
# 5. Deploy: vercel --prod
#
# Add multiple keys:
#    vercel env add OPENAI_API_KEY
#    vercel env add ANTHROPIC_API_KEY

# === NETLIFY DEPLOYMENT ===
# 1. Install Netlify CLI: npm install -g netlify-cli
# 2. Login: netlify login
# 3. Initialize: netlify init
# 4. Add environment variables:
#    netlify env:set GEMINI_API_KEY your_api_key_here
#    netlify env:set OPENAI_API_KEY your_openai_key_here
# 5. Deploy: netlify deploy --prod

# === ALTERVISTA DEPLOYMENT ===
# ⚠️ WARNING: AlterVista does NOT support environment variables!
# Your API keys will be EXPOSED in the client bundle.
# 
# For AlterVista (NOT RECOMMENDED for production):
# 1. Create .env.production with your API keys
# 2. Build: npm run build
# 3. Upload dist/ folder via FTP
# 4. CRITICAL: Set API usage limits in your provider's console
# 5. Restrict API keys to your AlterVista domain
# 6. Monitor usage regularly
#
# See ALTERVISTA_DEPLOYMENT.md for detailed instructions

# === GITHUB PAGES DEPLOYMENT ===
# ⚠️ WARNING: GitHub Pages does NOT support environment variables!
# Same limitations as AlterVista.
#
# For GitHub Pages (NOT RECOMMENDED for production):
# 1. Create .env.production with your API keys
# 2. Add to package.json: "homepage": "https://username.github.io/repo-name"
# 3. Build: npm run build
# 4. Deploy: npm run deploy (requires gh-pages package)

# === CUSTOM SERVER DEPLOYMENT ===
# For your own server (VPS, dedicated, etc.):
# 1. SSH into your server
# 2. Clone the repository
# 3. Create .env file with your keys
# 4. Build: npm run build
# 5. Serve dist/ folder with nginx/apache
# 6. Configure .htaccess or nginx.conf for SPA routing

# ----------------------------------------
# SECURITY CHECKLIST
# ----------------------------------------
# Before deploying to production:
# [ ] .env is in .gitignore (already done)
# [ ] No API keys hardcoded in source files
# [ ] API key usage limits set in provider console
# [ ] API keys restricted to production domain
# [ ] Monitoring/alerts enabled for API usage
# [ ] Using HTTPS in production
# [ ] Environment variables properly configured on hosting platform
# [ ] Consider using backend API proxy for key security
# [ ] Regular security audits scheduled

# ----------------------------------------
# TROUBLESHOOTING
# ----------------------------------------
# 
# Issue: "API_KEY not found in environment variables"
# Solution: 
#   - Ensure .env file exists in project root
#   - Restart dev server after creating .env
#   - Check variable name matches what's in the code
#   - For Vite, variables must start with VITE_ to be exposed to client
#     (but this project uses custom injection via vite.config.ts)
#
# Issue: API keys exposed in production
# Solution:
#   - Use Vercel/Netlify instead of AlterVista/GitHub Pages
#   - Or implement backend API proxy
#   - See WORKFLOW_GUIDE.md for backend setup
#
# Issue: API rate limit exceeded
# Solution:
#   - Set usage quotas in your provider's console
#   - Implement client-side rate limiting
#   - Cache API responses when possible
#   - Consider upgrading to paid tier
#
# Issue: Different API provider not working
# Solution:
#   - Check services/geminiService.ts for API integration
#   - You may need to modify the service to support your provider
#   - Ensure API key format matches provider requirements
#   - Check provider's documentation for correct endpoints

# ----------------------------------------
# NOTES
# ----------------------------------------
# - This project is configured for Google Gemini API by default
# - To use a different provider, you'll need to modify:
#   1. services/geminiService.ts (API integration)
#   2. vite.config.ts (environment variable injection)
# - Keep this file (.env.example) in version control
# - Never commit .env files to version control
# - Use different .env files for different environments:
#   .env.development (local development)
#   .env.production (production build)
#   .env.test (testing)

